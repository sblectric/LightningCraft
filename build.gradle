
// For those who want the bleeding edge
buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.2-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle.forge'

version = "2.7.0"
group= "com.sblectric.lightningcraft" // http://maven.apache.org/guides/mini/guide-naming-conventions.html
archivesBaseName = "lightningcraft"

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    maven { url "http://dvs1.progwml6.com/files/maven" }
	maven { url 'http://maven.epoxide.xyz' }
}

minecraft {
    version = "1.10.2-12.18.1.2094"
    runDir = "run"
    useDepAts = true
    mappings = "snapshot_20160518"
}

dependencies {
	deobfCompile "mezz.jei:jei_1.10.2:3.12.3.292"
    deobfCompile "slimeknights.mantle:Mantle:1.10.2-1.0.0.jenkins170"
    deobfCompile "slimeknights:TConstruct:1.10.2-2.5.2.jenkins352"
    compile "net.darkhax.tesla:Tesla:1.10.2-1.2.1.41"
}

processResources
{
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        // replace version and mcversion
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    // copy everything else, thats not the mcmod.info
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}
